# Testing Environment Configuration
# This file ensures tests NEVER use MySQL even if .env has MySQL credentials

APP_NAME="UC Laravel"
APP_ENV=testing
APP_KEY=base64:2fl+K9kGZYSY6ytAtpKr6EonZcgOkInGo9pWun/clVs=
APP_DEBUG=true

# CRITICAL: Use SQLite for testing
# This prevents accidental data loss if .env has MySQL production credentials
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Disable external services for testing
BROADCAST_CONNECTION=null
CACHE_STORE=array
CACHE_DRIVER=array
MAIL_MAILER=array
QUEUE_CONNECTION=sync
SESSION_DRIVER=array

# Performance
BCRYPT_ROUNDS=4

# Disable optional packages
PULSE_ENABLED=false
TELESCOPE_ENABLED=false
NIGHTWATCH_ENABLED=false
